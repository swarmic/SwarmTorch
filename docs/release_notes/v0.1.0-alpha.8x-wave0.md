# v0.1.0-alpha.8x-wave0 — Wave 8.0 Contract and Trust-Boundary Hardening

Status: Completed  
Date: 2026-04-05  
Scope: P0 conformance recovery, artifact manifest trust-boundary hardening, telemetry contract sync, and canon/security truth-sync

## Summary

Wave 8.0 closes the next hardening tranche after Wave 7.1:

1. Restores Rust 1.75 `no_std + alloc` core conformance gate by fixing trait-scope regression in `run_graph`.
2. Hardens artifact manifest validation against traversal/absolute/symlink escape paths.
3. Tightens required-manifest coverage for update logs consumed by report loading.
4. Aligns emitted telemetry attributes with ADR namespace contract (`swarmtorch.*`).
5. Reconciles canon/docs drift in remediation backlog, whitepaper, and models crate docs.
6. Pins CI third-party actions to immutable SHAs.
7. Documents replay limitation operating controls and clarifies `SecurityConfig` non-enforcing scope.

## Primary Code Changes

1. `swarm-torch-core/src/run_graph.rs`
2. `swarm-torch/src/artifacts/bundle.rs`
3. `swarm-torch/src/artifacts/tests.rs`
4. `swarm-torch/src/scheduler.rs`
5. `swarm-torch/src/native_runner.rs`
6. `swarm-torch-core/src/crypto.rs`

## Canon / CI / Security Changes

1. `.github/workflows/rust.yml` (third-party action SHA pinning)
2. `SECURITY.md` (replay operational controls + SecurityConfig enforcement scope clarity)
3. `docs/plans/v0.1.0-alpha.6x-remediation-backlog.md` (R9 state matrix closure)
4. `SWARM_TORCH_TECHNICAL_WHITE_PAPER_v0.1.md` (robustness/embedded status updates)
5. `swarm-torch-models/src/lib.rs` (ONNX capability wording corrected)
6. `docs/plans/v0.1.0-alpha.8x-phase2-remediation-roadmap.md` (deferred near-term hardening backlog)

## New / Updated Tests

1. `validate_manifest_rejects_parent_traversal_path`
2. `validate_manifest_rejects_absolute_path`
3. `validate_manifest_rejects_symlink_escape` (unix)
4. `validate_manifest_accepts_legitimate_nested_path`
5. `validate_manifest_requires_registry_updates_ndjson`
6. `scheduler_events_use_swarmtorch_namespace`
7. `native_runner_spans_use_swarmtorch_namespace`

## Migration Notes

### Telemetry attribute key rename

Wave 8.0 renames scheduler/native-runner emitted SwarmTorch-defined keys to ADR-compliant names:

- `node_key` -> `swarmtorch.node_key`
- `detail` -> `swarmtorch.detail`
- `op_type` -> `swarmtorch.op_type`
- `input_count` -> `swarmtorch.input_count`
- `output_count` -> `swarmtorch.output_count`

Consumers parsing run artifacts should update key lookups accordingly.

## Acceptance Gates

1. `cargo fmt --all -- --check`
2. `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
3. `cargo test --workspace --locked`
4. `cargo +1.75 build -p swarm-torch-core --no-default-features --locked`
5. `cargo +1.75 build -p swarm-torch-core --no-default-features --features alloc --locked`
6. `cargo run -p swarm-torch --example artifact_pipeline --locked`

All passed on 2026-04-05.
