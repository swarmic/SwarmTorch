# ESP32 Contributor Node (Planned)

Goal: demonstrate a resource-constrained contributor that:

- Computes a local update (or compressed sketch)
- Emits bounded telemetry/events suitable for gateways
- Operates under the `embedded_min` / `embedded_alloc` profiles

Status: placeholder only.
