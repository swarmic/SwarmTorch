# v0.1.0-alpha.7x-wave1 — Wave 7.1 Reliability Convergence (R9)

Status: Completed  
Date: 2026-03-11  
Scope: Scheduler fail-closed hardening, graph invariant enforcement, and canon/release-trace reconciliation

## Summary

This wave closes post-Wave7 reliability gaps identified in remediation backlog `R9`:

1. Adds graph-scope validation (`validate_graph_v1`) with typed errors for duplicate node keys/ids and unknown edge endpoints.
2. Removes production panic paths from scheduler topological sort and replaces them with typed `SchedulerError` fail-closed returns.
3. Enforces graph validation on artifact write path (`RunArtifactBundle::write_graph`) before and after normalization.
4. Repairs canon/docs drift (ADR-0017, roadmap, whitepaper, README) to reflect current Wave7 implementation reality.
5. Restores release-note evidence chain integrity via archival bridge note `v0.1.0-alpha.6x-wave2.1.md`.

## Primary Code Changes

1. `swarm-torch-core/src/run_graph.rs`
2. `swarm-torch/src/scheduler.rs`
3. `swarm-torch/src/artifacts/bundle.rs`
4. `swarm-torch-core/src/lib.rs` (prelude export)

## Canon/Docs Changes

1. `ADRs.md` (ADR-0017 implementation/schema status)
2. `ROADMAP.md` (Wave6/Wave7 status sync)
3. `SWARM_TORCH_TECHNICAL_WHITE_PAPER_v0.1.md` (execution/schema status sync)
4. `README.md` (roadmap status wording)
5. `docs/release_notes/v0.1.0-alpha.6x-wave2.1.md` (archival bridge)

## New/Updated Tests

1. `validate_graph_rejects_duplicate_node_keys`
2. `validate_graph_rejects_duplicate_node_ids`
3. `validate_graph_rejects_unknown_edge_endpoints`
4. `scheduler_rejects_duplicate_node_ids`
5. `scheduler_rejects_duplicate_node_keys`
6. `scheduler_invalid_graph_returns_error_not_panic`

## Acceptance Gates

1. `cargo fmt --all -- --check`
2. `cargo check --workspace --all-targets`
3. `cargo clippy --workspace --all-targets --all-features -- -D warnings`
4. `cargo test --workspace`

All passed on 2026-03-11.
